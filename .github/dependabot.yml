version: 2
updates:
  - package-ecosystem: github-actions
    directory: "/"
    schedule:
      interval: weekly
    groups:
      # init/autobuild/analyze は同一バージョンでないと実行時に設定ファイルの不一致で失敗するため、1つの PR にまとめる
      codeql-action:
        patterns:
          - "github/codeql-action*"
