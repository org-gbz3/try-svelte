---
name: pre-design-md output
description: Generated by pre-design-md
colors:
  primary: "#0091ce"
  primary-50: "#edfdff"
  primary-100: "#d8f7ff"
  primary-200: "#ace7ff"
  primary-300: "#76d0ff"
  primary-400: "#3bb1eb"
  primary-500: "#0091ce"
  primary-600: "#0072aa"
  primary-700: "#005481"
  primary-800: "#00385a"
  primary-900: "#001e33"
  primary-950: "#000d1c"
  neutral-50: "#fafafa"
  neutral-100: "#f2f2f2"
  neutral-200: "#dedede"
  neutral-300: "#c4c4c4"
  neutral-400: "#a4a4a4"
  neutral-500: "#868686"
  neutral-600: "#696969"
  neutral-700: "#4d4d4d"
  neutral-800: "#333333"
  neutral-900: "#1b1b1b"
  neutral-950: "#0b0b0b"
  success: "#46944b"
  warning: "#d09a30"
  danger: "#c35c57"
  info: "#1289c7"
typography:
  h1:
    fontFamily: "'Inter', system-ui, -apple-system, sans-serif"
    fontSize: 3.0518rem
    fontWeight: 700
    lineHeight: 1.275
  h2:
    fontFamily: "'Inter', system-ui, -apple-system, sans-serif"
    fontSize: 2.4414rem
    fontWeight: 700
    lineHeight: 1.275
  h3:
    fontFamily: "'Inter', system-ui, -apple-system, sans-serif"
    fontSize: 1.9531rem
    fontWeight: 700
    lineHeight: 1.275
  h4:
    fontFamily: "'Inter', system-ui, -apple-system, sans-serif"
    fontSize: 1.5625rem
    fontWeight: 700
    lineHeight: 1.275
  body:
    fontFamily: "'Inter', system-ui, -apple-system, sans-serif"
    fontSize: 1rem
    fontWeight: 400
    lineHeight: 1.58
  caption:
    fontFamily: "'Inter', system-ui, -apple-system, sans-serif"
    fontSize: 0.8rem
    fontWeight: 400
    lineHeight: 1.58
spacing:
  2xs: 2px
  xs: 4px
  sm: 6px
  md: 8px
  lg: 12px
  xl: 16px
  2xl: 24px
  3xl: 32px
  4xl: 48px
  5xl: 64px
rounded:
  none: 0
  sm: 4px
  md: 8px
  lg: 12px
  xl: 16px
  full: 9999px
components:
  button:
    rounded: "{rounded.md}"
  card:
    rounded: "{rounded.lg}"
  input:
    rounded: "{rounded.sm}"
  badge:
    rounded: "{rounded.full}"
---

## Overview

Brand intent: blue-led, balanced.

Target context: general — adapt the values verbatim and elaborate on rationale where helpful.

Decisions made: typography, spacing, radius, shadow, color.

Dark mode is supported and dark variants of the palette are part of this spec.

## Colors

Color approach: blue-led, balanced.

A blue-led palette, confident and modern, paired with objective, neutral grays. The hex values in the frontmatter are derived from OKLCH source values; if you need the OKLCH form, see the Rich Prompt output.

### Interaction states (derived rules)

- `hover`: lightness −5%
- `active`: lightness −10%
- `focus`: 2px solid `{colors.primary}` with 2px offset
- `disabled`: opacity 40%

## Typography

Base font size: 16px. Modular scale ratio: 1.25 (major third).

Single-family system (Modern Sans) — heading and body share the typeface for unity.

Inter throughout — neutral, dense, universal.

## Spacing

Base unit: 4px. Scale approach: linear.

Tight, precise grid — data-dense surfaces with consistent rhythm.

## Rounded

Base radius: 8px (friendly and modern). Scale approach: scaled.

Per-component variation reinforces shape roles — inputs crisper, cards softer.

Per-component intent (resolved in the components block):
- input: `{rounded.sm}`
- button: `{rounded.md}`
- card: `{rounded.lg}`
- badge: `{rounded.full}`

## Components

Component-level shape choices are resolved via token references in the `components` block of the frontmatter. Future component additions should follow the same pattern: declare the role (button, card, input, …) and reference a `rounded` token rather than hard-coding a px value.

## Shadow

Shadow is not in the official YAML schema at alpha. Tokens are emitted as CSS variables for direct consumption:

```css
--shadow-sm: 0 1px 2px 0 oklch(0% 0 0 / 0.09);
--shadow-md: 0 4px 6px -1px oklch(0% 0 0 / 0.13), 0 2px 4px -2px oklch(0% 0 0 / 0.13);
--shadow-lg: 0 10px 15px -3px oklch(0% 0 0 / 0.18), 0 4px 6px -4px oklch(0% 0 0 / 0.18);
--shadow-xl: 0 20px 25px -5px oklch(0% 0 0 / 0.22), 0 8px 10px -6px oklch(0% 0 0 / 0.22);
```

Intensity: medium. Neutral black, no tinting.

Elevation hierarchy: `dropdown` (sm) < `card` (md) < `modal` (lg) < `toast` (xl).

## Usage guidelines

- Primary actions use the `primary` color; on hover shift to `primary-600`, on active to `primary-700`.
- Cards take the `card` rounded role and a medium shadow.
- Buttons take the `button` rounded role and a small shadow on the primary variant.
- Inputs take the `input` rounded role; focus ring follows the interaction-states spec.
- Body text uses the `body` typography role.
- Section spacing: use the `3xl` spacing token between major blocks.
- Stack spacing within a block: use the `md` spacing token.

## Generation instructions for AI

1. Treat the listed values as authoritative; do not re-derive from preference.
2. When a component needs a value not listed here, pick the nearest available token.
3. If a new decision is required, document it in an appended section and flag it clearly.
4. Preserve the rationale comments — they encode design intent.
5. Interaction states are derived rules, not hard-coded values — apply them procedurally.
